package com.hearopilot.app.domain.model

/**
 * Theme mode setting for the app.
 *
 * - SYSTEM: Follow system dark mode setting (default)
 * - LIGHT: Always use light theme
 * - DARK: Always use dark theme
 */
enum class ThemeMode {
    SYSTEM,
    LIGHT,
    DARK
}
